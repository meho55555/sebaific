#include <stdio.h>
#include <stdlib.h>
#define ROZM 100

int main()
{
    char *pstr=NULL;
    int i, dlugosc;
    pstr=malloc(ROZM * sizeof(*pstr));
    printf("Podaj napis\n");
    fgets(pstr,ROZM,stdin);
    for(i=0; i<dlugosc ; i++)
    {
        printf("%c", pstr[i]);
    }
    free(pstr);
    return 0;
