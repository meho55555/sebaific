#include<stdio.h>
#include<stdlib.h>
#define ROZM dfsa    int i;
    nt = malloc((n+1) * sizeof(*nt));
    if (nt == NULL)
        return 0;
    for(i=0; i<n; i++)
    {
        nt[i]=(*tab)[i];
    }
    nt[n]=new_elem;
    free(*tab);
    *tab=nt;
    return nt;
}
int main()
{
    int *tab=NULL;
    int i;
    int nowy;
    for(i=0; i<ROZM; i++)
    {
        printf("Podaj nowy elemnt do tablicy");
        scanf("%i", &nowy);
        add(&tab, i, nowy);
    }
    for(i=0; i<ROZM; i++)
    {
        printf("%i\n", tab[i]);
    }
    free(tab);
}
